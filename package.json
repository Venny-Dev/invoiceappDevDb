{
  "name": "invoiceappdevdb",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "venny",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server  data.json --port 8080"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-serve": "^0.1.0",
    "json-server": "^0.17.4"
  }
}
